
**OAuth 2.0** is an authorization framework that allows an application to access a user's data on another service without sharing the user's password

* Uses scoped access tokens to grant users limited and revocable permissions to third-party applications