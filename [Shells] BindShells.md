# Cybersecurity: Bind Shells

A bind shell opens a port on the target machine to have it listen for the attacker's incoming connection to gain a remote terminal

* Turns the compromised system into a server awaiting commands