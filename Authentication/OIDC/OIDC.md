
**OpenID Connect** is an authentication protocol build on top of [[OAuth 2.0]] to help allow applications verify the identity of a user

* Provides a standard way to authenticate users and return user identity information